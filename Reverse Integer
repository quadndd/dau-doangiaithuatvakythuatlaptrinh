class Solution(object):
    def reverse(self, x):
        s = str(abs(x))
        reversed_s = s[::-1]
    
        res = int(reversed_s)
        if x < 0:
            res = -res
        
        if res < -2**31 or res > 2**31 - 1:
            return 0
            
        return res